{{!-- {{#each posts}}
  {{#if (equal user_id post.user_id)}}
    <section class="post">
      <header class="post-header">
        <h2>{{post.title}}</h2>
      </header>
      <div class="post-content">
        <p class="author">{{post.User.username}}</p>
        <p class="date">Published: {{post.createdAt}}</p>
        <p>{{post.content}}</p>
        <a href="/posts/{{post.id}}">Read more</a>
      </div>
    </section>
  {{/if}}
{{/each}} --}}

{{#each posts as |post|}}
  {{#if (eq post.user_id user_id)}}
    <section class="post">
      <header class="post-header">
        <h2>{{post.title}}</h2>
      </header>
      <div class="post-content">
        <p class="author">{{post.User.username}}</p>
        <p class="date">Published: {{post.createdAt}}</p>
        <p>{{post.content}}</p>
        <a href="/posts/{{post.id}}">Read more</a>
      </div>
    </section>
  {{/if}}
{{/each}}