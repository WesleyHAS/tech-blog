{{!--
{{#each posts as |post|}}
  {{#if (eq post.user_id user_id)}}
    <section class="post">
      <header class="post-header">
        <h2>{{post.title}}</h2>
      </header>
      <div class="post-content">
        {{#with (findUser post.user_id users) as |author|}}
          <p class="author">{{author.username}}</p>
        {{/with}}
        <p class="date">Published: {{post.createdAt}}</p>
        <p>{{post.content}}</p>
        <a href="/posts/{{post.id}}">Read more</a>
      </div>
    </section>
  {{/if}}
{{/each}} --}}

{{!-- {{#each users as |user|}}
  {{#if (eq user.user_id user_id)}}
    <section class="post">
      <header class="post-header">
        <h2>{{user.post.title}}</h2>
      </header>
      <div class="post-content">
        <p class="author">{{username}}</p>
        <p class="date">Published: {{user.post.createdAt}}</p>
        <p>{{user.post.content}}</p>
        <a href="/posts/{{post.id}}">Read more</a>
      </div>
    </section>
  {{/if}}
{{/each}} --}}

{{#each posts as |post|}}
  {{#if (eq post.user_id user_id)}}
    <section class="post">
      <header class="post-header">
        <h2>{{post.title}}</h2>
      </header>
      <div class="post-content">
        <p class="author">{{post.user.username}}</p>
        <p class="date">Published: {{post.createdAt}}</p>
        <p>{{post.content}}</p>
        <a href="/api/posts/{{post.id}}">Read more</a>
      </div>
    </section>
  {{/if}}
{{/each}}